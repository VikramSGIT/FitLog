// Global UI/configuration settings
// Autosave delay for text fields, comments, and set edits (in milliseconds)
export const AUTO_SAVE_DELAY_MS = 60000
export const DEFAULT_REST_DURATION_SECONDS = 90
export const NOTE_COMMIT_DELAY_MS = 2000
